Check if the llama3 model and mxbai-embed-large are installed locally


Start the llama3 model locally and check if it's live by navigating to this URL : http://localhost:11434


Create a .venv and install the required libraries/packages listed in requirements.txt


Since this is run locally, you won't be needing any API keys or .env.
